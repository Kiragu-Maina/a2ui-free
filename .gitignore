# --- Secrets (NEVER commit) -------------------------------------------------
.env
.env.local
.env.test
.env.*.local
*.pem
*.key
secrets/

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
env/
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
htmlcov/
build/
dist/

# --- Node -------------------------------------------------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
.yarn/
package-lock.json.bak

# --- IDE / editor -----------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# --- council-test workspace -------------------------------------------------
# Test scratch dir — keep scripts/configs, ignore secrets, venv, generated artifacts.
council-test/venv/
council-test/discovered_models.json
council-test/probe_results.json
council-test/probe_round2.json
council-test/groq_cere.json
council-test/cohere_probe.json
council-test/nvidia_models.json
council-test/recovered_live.json
council-test/groq_models.json
council-test/cere_models.json
# Local copies of remote files (for editing only)
council-test/shellwire_constants.ts
council-test/health-monitor.service.ts

# --- Downloaded packages (re-downloadable via `npm pack` / `pip download`) --
packages/

# --- slides-download workspace ---------------------------------------------
# Keep the PNGs and the puppeteer script; ignore the Node venv.
slides-download/node_modules/
slides-download/package-lock.json
